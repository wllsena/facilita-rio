services:
  app:
    build: .
    restart: always
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./evaluation/results:/app/evaluation/results
